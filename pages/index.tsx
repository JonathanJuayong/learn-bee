import { Button, Heading, Text } from '@chakra-ui/react';
import { Navbar } from '../components/common';

const Home: React.FC = () => {
  return (
    <div>
      <Navbar />
    </div>
  )
}

export default Home;